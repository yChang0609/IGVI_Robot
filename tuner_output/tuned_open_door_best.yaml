# Captured by wildbot_grasp tune_open_door — merge into open_door_server params.
open_door_server:
  ros__parameters:
    ready_distance_m: 0.252
    door_home_pose_deg:  [+121.920, +74.880, +236.880]  # UNSET — not captured
    door_above_pose_deg: [0.0, 0.0, 0.0]  # UNSET — not captured
    door_press_pose_deg: [0.0, 0.0, 0.0]  # UNSET — not captured
    door_push_pose_deg:  [0.0, 0.0, 0.0]  # UNSET — not captured
